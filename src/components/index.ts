import Modal, {
  ModalProps,
  ModalBackground,
  ModalContainer,
  ModalChildren,
  CloseButton,
} from "./Modal";
import CloseIcon from "./Close";

export default Modal;
export {
  ModalBackground,
  ModalContainer,
  ModalChildren,
  CloseIcon,
  CloseButton,
};
export type { ModalProps };
